#include <stdio.h>
#include <string.h>

int main() {
    FILE *fp;
    char filename[50], pattern[50], line[200];

    printf("Enter file name: ");
    scanf("%s", filename);

    printf("Enter pattern to search: ");
    scanf("%s", pattern);

    fp = fopen(filename, "r");
    if (fp == NULL) {
        perror("Error opening file");
        return 1;
    }

    printf("\nLines containing '%s':\n", pattern);

    // Read file line by line
    while (fgets(line, sizeof(line), fp)) {
        if (strstr(line, pattern)) {  // if pattern found in line
            printf("%s", line);
        }
    }

    fclose(fp);
    return 0;
}

INPUT:
UNIX is a powerful operating system.
Learning C under UNIX is useful.
System calls are important in UNIX.
This line does not contain the word.

Enter file name: sample.txt
Enter pattern to search: UNIX

OUTPUT:
Lines containing 'UNIX':
UNIX is a powerful operating system.
Learning C under UNIX is useful.
System calls are important in UNIX.
